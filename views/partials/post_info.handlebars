<div class="card">
  <div class="card-header">
    {{title}}
  </div>
  <div class="card-body">
    <h5 class="card-title">{{user.name}}</h5>
    <p class="card-text">{{content}}</p>
    <a href="/post/{{id}}" class="btn btn-primary">View Comments</a>
  </div>
</div>